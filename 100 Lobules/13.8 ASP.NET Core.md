---
note_type: mn
tags:
  - programming
  - c#
  - web_development
  - .net
mn:
  - "[[13.7 C Sharp]]"
  - "[[12 Web Dev]]"
---
[[13.7 C Sharp|C#]]

# Table of content
## Introduction
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "introduction")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

## Templates
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "templates")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

## Configuring the app
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "configuration")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

## Middleware
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "middleware")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

## Kestrel
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "kestrel")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

## Services
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "services")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

## DI
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "dependency_injection")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

## MVC
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "mvc")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```
### Model
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "mvc") and contains(tags, "model")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```
### Controller
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "mvc") and contains(tags, "controller")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```

### View
```dataview 
TABLE WITHOUT ID link(file.name, short) AS "Note",  dateformat(file.ctime,"yyyy-MM-dd") AS "Creation date", question AS "Questions"
FROM "200 Neurons"
WHERE contains(file.name, "23.8") and contains(tags, "view") and contains(tags, "mvc")
FLATTEN substring(file.name, 5) AS short
SORT file.ctime ASC
```
# Resources

